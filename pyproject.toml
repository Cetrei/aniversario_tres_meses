[project]
name = "tres-meses-aniversario"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "qr-generator" 
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
qr-generator = { workspace = true }