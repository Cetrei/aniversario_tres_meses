Coloca aquí tu archivo de música de fondo con el nombre `musica.mp3`
(o ajusta la ruta en `src/config.ts` -> `backgroundMusic.src` si usas otro nombre/formato).

Formatos soportados por el navegador: mp3, ogg, wav, m4a.
Recomendado: mp3, comprimido, menos de ~5MB para que cargue rápido.
